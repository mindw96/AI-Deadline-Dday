# Privacy

The initial version of Dday is local-first.

- No account is required.
- No analytics are collected.
- No advertising SDK is included.
- No user settings are sent to a server.
- Selected conference settings are stored locally with `UserDefaults`.

Future remote data update support may fetch public JSON from GitHub. If that feature is added, this document should be updated before release.
